name: External Payment Orchestration Tests

on:
  pull_request:
    paths:
      - "src/lib/external-integrations/payments/**"
      - "src/app/api/integrations/payments/**"
      - "supabase/migrations/*business_payment_methods.sql"
      - "tests/external-payment-orchestration.test.mjs"
      - "tsconfig.external-payments.json"
      - "docs/external-payment-orchestration.md"
      - ".github/workflows/external-payment-sandbox-tests.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  external-payment-sandbox:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Type-check payment orchestration core
        run: bun x tsc -p tsconfig.external-payments.json --pretty false

      - name: Run orchestration contracts
        run: bun test tests/external-payment-orchestration.test.mjs
