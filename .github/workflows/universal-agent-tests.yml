name: Universal Agent Tests

on:
  pull_request:
    paths:
      - "src/lib/engine-universal*.ts"
      - "src/lib/universal-*.ts"
      - "src/app/api/workflows/execute/route.ts"
      - "src/components/editor/editor-view-gemini.tsx"
      - "tests/universal-agent-*.test.mjs"
      - ".github/workflows/universal-agent-tests.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  universal-agent:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run universal agent contracts
        run: bun test tests/universal-agent-contract.test.mjs tests/universal-agent-scenarios.test.mjs tests/universal-session-memory.test.mjs tests/universal-agent-core.test.mjs tests/universal-agent-stable-freeze.test.mjs tests/universal-agent-memory-reset.test.mjs tests/universal-agent-empty-cart-total.test.mjs
