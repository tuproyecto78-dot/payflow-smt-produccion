# Validación ejecutada

```text
node --experimental-strip-types --test tests/business-context.test.mjs
```

Resultado local:

- 7 pruebas aprobadas;
- 0 fallidas;
- saludo con identidad del negocio;
- catálogo con productos y precios exactos;
- promociones reales;
- pagos sin confirmación ni ejecución;
- instrucciones completas para Gemini;
- saneamiento de reglas heredadas;
- eliminación de menciones de la plataforma en la respuesta al cliente.

También se validaron los archivos puros con TypeScript en modo estricto.
